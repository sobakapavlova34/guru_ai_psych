По мета пользователя и числу срабатываний trigger_words выбери ОДИН наиболее подходящий тип проблемы.

Кандидаты (одинаковый счёт по триггерам):
{{opts}}

Мета (JSON):
{{meta_blob}}

Верни СТРОГО JSON: {"problem_key": "<один из ключей кандидатов дословно>"}
